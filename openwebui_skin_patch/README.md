# Deprecated Scratch Workspace

This directory is an earlier scratch area and is no longer canonical.

Use the maintained patch bundle instead:

- `/home/ubuntu/vllm_jetson/bartlebyGPT/openwebui_skin/README.md`

That bundle provides the update-friendly workflow:

1. overlay sync
2. patch stack apply
3. patch refresh after upstream changes
